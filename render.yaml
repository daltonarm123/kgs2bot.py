services:
  - type: worker
    name: kg2recon
    env: python
    startCommand: python kg2bot.py
    autoDeploy: true
    envVars:
      - key: DISCORD_TOKEN   # set the value in Render dashboard (do NOT commit it)
        sync: false
      - key: DATABASE_URL    # Postgres connection string
        sync: false
